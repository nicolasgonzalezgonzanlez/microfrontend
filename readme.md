# Poc Microfrontend 
### create-single-spa

### Install

```
npm install --global create-single-spa
cd ... 
npm install

```
#Poc Microfrontend 
## To see the architecture of the project enter here
[create-single-spa](https://single-spa.js.org/)

### **Config**:
> Install create-simple-spa
```
npm install --global create-single-spa
```
### **Instalation**:
> Install all dependencies
```sh
npm run install
```
> To start the execution of the project in development mode
```sh
npm run start
```
> Note
Run root-app or root-engine after run backo and navbar
